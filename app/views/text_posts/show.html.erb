<%= form_for(@text_post) do |f| %>

  <div class="field">
    <%= f.label :url %><br>
    <%= f.text_field :url %>
  </div>

  <div class="field">
    <%= f.label :title %><br>
    <%= f.text_field :title %>
  </div>

  <div class="field">
    <%= f.label :blog %><br>
    <%= f.text_field :blog %><br>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
<% if @errors %>
  <div id="error_explanation">
  <h2><%= pluralize(@errors.count, "error") %> prohibited this text post from being saved:</h2>
    <ul>
      <% @errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
    </ul>

  </div>
    <p>
        <%= link_to "New Text Post", new_text_post_path %>
        <%= link_to "Edit", edit_text_post_path(@text_post) %>
        <%= link_to "Delete", text_post_path(@text_post),
          data: {:confirm => 'Are you sure?'}, :method => :delete %>
        <%= link_to "Back", text_posts_path %>
    </p>
 <% end %>
